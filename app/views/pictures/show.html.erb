<p id="notice"><%= notice %></p>

<p>
  <strong>Image:</strong>
  <%= @picture.image %>
</p>

<p>
  <strong>Content:</strong>
  <%= @picture.content %>
</p>
<p>投稿者：<%= @picture.user.name %></p>
<%= link_to 'Edit', edit_picture_path(@picture) %> 
<%= link_to 'Destroy', picture_path, method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to 'Back', pictures_path %>
